
// BMac: added this file

module.exports = function(THREE) {
  function ClearColorPass() {
    if (!(this instanceof ClearColorPass)) return new ClearColorPass(scene, camera);
    this.enabled = true;
  };

  ClearColorPass.prototype = {
    render: function ( renderer, writeBuffer, readBuffer, delta ) {
      renderer.clearTarget(writeBuffer, true, false, false);
      renderer.clearTarget(readBuffer, true, false, false);
    }
  };

  return ClearColorPass
};